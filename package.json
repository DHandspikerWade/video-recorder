{
  "name": "video-recorder",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "dependencies": {
    "@kubernetes/client-node": "^1.1.2",
    "docker-events": "^0.0.2",
    "dockerode": "^2.0.3",
    "mqtt": "^4.2.8",
    "redis": "^4.2.0"
  },
  "scripts": {
    "update-lock": "docker run -v $(pwd):/app --rm -it node:22-alpine sh -c 'cd /app && npm update'"
  },
  "author": "",
  "license": "ISC"
}
